import Route from "../interface/routes.interface";
declare class ReferalsRoute implements Route {
    router: import("express-serve-static-core").Router;
    constructor();
    private initializeRoutes;
}
export default ReferalsRoute;
